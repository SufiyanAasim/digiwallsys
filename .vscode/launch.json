{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run web application",
      "command": "npm run web --workspace @digiwallsys/mobile",
      "cwd": "${workspaceFolder}"
    }
  ]
}
