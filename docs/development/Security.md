# Security engineering

Use parameterized SQL, server-derived identities, short-lived tokens, input
validation, and atomic writes. Before production, add security headers, rate
limits, refresh-token rotation, secure mobile key storage, audit events,
dependency scanning, SAST, DAST, secret scanning, and threat-model reviews.

See the root `SECURITY.md` for private reporting instructions.
